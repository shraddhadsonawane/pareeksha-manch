<%= form_for(sub_topic) do |f| %>
  <% if sub_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sub_topic.errors.count, "error") %> prohibited this sub_topic from being saved:</h2>

      <ul>
      <% sub_topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_area :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :topic_id %>
    <%= f.select :topic_id, options_for_select(Topic.all.collect{ |t| [t.title, t.id] }, sub_topic.topic_id) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
